<md-toolbar>
	<div class="md-toolbar-tools">
		<md-button ng-click="toggleSidenav('left')" hide-gt-md aria-label="Menu">
			<ng-md-icon icon="menu"></ng-md-icon>
		</md-button>
		<h3>
			Rails Ninja
		</h3>
		<span flex></span>
		<md-button aria-label="Open Settings" ng-click="showListBottomSheet($event)">
			<ng-md-icon icon="more_vert"></ng-md-icon>
		</md-button>
	</div>
</md-toolbar>

<div layout="row" layout-align="center center">
	
	<div class="add-button">
		<md-button class="md-fab md-fab-bottom-right" aria-label="Add" ng-click="addTask($event)">
			<ng-md-icon icon="add"></ng-md-icon>
		</md-button>
	</div>

	<div flex="50">
		<md-content flex md-scroll-y>

			<md-input-container flex>
				<label>Search</label>
				<input type="text" ng-model="searchTask" placeholder="Search a task here">
			</md-input-container>

			<div class="inset" hide-sm></div>
				<md-list>
					<md-item ng-repeat="task in tasks | filter:searchTask | orderBy:updatedAt">
						<md-item-content>
							<md-card flex-gt-sm="90" flex-gt-md="80">
								<div class="md-tile-content">
									<h3>{{task.title}}</h3>
									<h4>{{task.description | limitTo:25}}{{task.description.length > 25? "..." : ""}}</h4>
									<p>
										{{task.total | currency:"$":0}}
									</p>
									<md-button ng-click="editTask(task)" class="md-primary">
										Edit
									</md-button>
								</div>
							</md-card>
						</md-item-content>
					</md-item>
				</md-list>
			</div>
		</md-content>
	</div>

</div>